model: mlx-community/Qwen3-4B-Instruct-2507-4bit
train: true
fine_tune_type: lora
optimizer: adamw
data: data
seed: 42
num_layers: 16
batch_size: 1
iters: 300
val_batches: -1
learning_rate: 0.00005
steps_per_report: 5
steps_per_eval: 25
grad_accumulation_steps: 1
adapter_path: adapters/summarizer
save_every: 50
test: false
max_seq_length: 2048
grad_checkpoint: true
mask_prompt: true
lora_parameters:
  keys: ["self_attn.q_proj", "self_attn.v_proj"]
  rank: 8
  scale: 16.0
  dropout: 0.0
